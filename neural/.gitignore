# Self-contained ignore rules for the neural/ sub-project.
# Keeps large/derived/licensed artifacts out of git without touching the parent
# repo's .gitignore.

# Trained checkpoints and exported encoders
*.pt
*.safetensors
checkpoints/
runs/

# Aggregated / derived corpora (regenerated by aggregate.py; default lives in
# ~/.cache/syriac-neural but allow a local ./data too)
data/
*.jsonl
*.arrow

# SEDRA-derived training pairs. SEDRA 3 is "MIT with restrictions": academic use
# only, NO redistribution of altered versions. We never commit SEDRA-derived
# data; it is regenerated locally from a user-provided SEDRA source. See
# docs/DATA.md.
sedra_cache/
*.sedra.json

# Python noise
__pycache__/
*.pyc
.ipynb_checkpoints/
